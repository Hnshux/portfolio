// JavaScript File - script.js
// Aap yahan apna interactive portfolio logic likh sakte hain.

document.addEventListener("DOMContentLoaded", () => {
    console.log("Hanshu's Portfolio is successfully loaded!");
    
    // Example: Click effect for mobile tab items
    const tabItems = document.querySelectorAll('.tab-item');
    tabItems.forEach(item => {
        item.addEventListener('click', () => {
            // Future active state logic can be added here
        });
    });
});
